import React from 'react';
import PropTypes from 'prop-types';

Upload.propTypes = {
    
};

function Upload(props) {
    return (
        <div>
            Upload
        </div>
    );
}

export default Upload;